import React from "react";
import { Box, Typography } from "@mui/material";

const ComponentD = () => {
  return (
    <Box
      sx={{
        backgroundColor: "white",
        border: "1px solid black",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
      }}
    >
      <Typography>Component D</Typography>
    </Box>
  );
};

export default ComponentD;
